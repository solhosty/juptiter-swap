/// <reference types="node" />
import BN from "bn.js";
import { PublicKey } from "@solana/web3.js";
export declare const OB_LAYOUT: import("@solana/buffer-layout").Structure<any>;
export interface Order {
    key: BN;
    owner: PublicKey;
    quantity: BN;
    feeTier: number;
    clientOrderId: BN;
    total: BN;
}
export declare class OrderBook {
    private header;
    nodes: any;
    constructor(header: any, nodes: any);
    get isDescending(): boolean;
    static decode(buffer: Buffer): OrderBook;
    static getPriceFromKey(key: BN): any;
    get(searchKey: BN | number): Order | null;
    get items(): Order[];
    allItems(descending?: boolean): Order[];
    find(clientOrderId: BN | number): Order | null;
    findMinMax(isMax: boolean): Order | null;
}
export declare const ORDER_SIZE: number;
//# sourceMappingURL=order.d.ts.map