import { Cluster, PublicKey, Transaction, TransactionInstruction } from '@solana/web3.js';
import BN from 'bn.js';
import { AffiliateVaultProgram, VaultProgram, VaultState } from '../types';
import { Strategy, StrategyHandler } from '.';
export default class CypherHandler implements StrategyHandler {
    private cypherClient;
    private cluster;
    private program;
    constructor(cluster: Cluster, program: VaultProgram);
    withdraw(walletPubKey: PublicKey, program: VaultProgram, strategy: Strategy, vault: PublicKey, tokenVault: PublicKey, vaultState: VaultState, userToken: PublicKey, userLp: PublicKey, amount: BN, preInstructions: TransactionInstruction[], postInstructions: TransactionInstruction[], opt?: {
        affiliate?: {
            affiliateId: PublicKey;
            affiliateProgram: AffiliateVaultProgram;
            partner: PublicKey;
            user: PublicKey;
        };
    }): Promise<Transaction>;
}
//# sourceMappingURL=cypher.d.ts.map