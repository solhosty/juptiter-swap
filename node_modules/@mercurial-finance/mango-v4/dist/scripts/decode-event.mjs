import {
  IDL
} from "../chunk-Z2RB6KEX.mjs";
import "../chunk-5VRACIDE.mjs";

// ts/client/src/scripts/decode-event.ts
import { BN, BorshCoder } from "@project-serum/anchor";
async function main() {
  const coder = new BorshCoder(IDL);
  const event = coder.events.decode(process.argv[2]);
  console.log(
    JSON.stringify(
      event,
      function(key, value) {
        const orig_value = this[key];
        if (orig_value instanceof BN) {
          return orig_value.toString();
        }
        return value;
      },
      "    "
    )
  );
  process.exit();
}
main();
//# sourceMappingURL=decode-event.mjs.map