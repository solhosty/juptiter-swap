import { Connection, PublicKey, TransactionInstruction } from '@solana/web3.js';
import { Instruction, Owner } from './utils';
export declare function createOpenOrdersInstruction(serumMarket: PublicKey, serumProgramId: PublicKey, userTransferAuthority: PublicKey): [PublicKey, TransactionInstruction];
export declare function getOrCreateOpenOrdersAddress(connection: Connection, user: PublicKey, serumMarket: PublicKey, serumProgramId: PublicKey): Promise<(Instruction & {
    address: PublicKey;
}) | undefined>;
export declare function getAtaInstructions({ connection, outputMints, owner, unwrapSOL, }: {
    connection: Connection;
    outputMints: PublicKey[];
    owner: Owner;
    unwrapSOL: boolean;
}): Promise<{
    userIntermediaryTokenAccountResults: (Instruction & {
        address: PublicKey;
    })[];
    userDestinationTokenAccountResult: Instruction & {
        address: PublicKey;
    };
}>;
