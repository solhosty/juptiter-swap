import { FeeCalculator } from '@solana/web3.js';
import { SetupInstructions, TransactionFeeInfo } from './types';
export declare const calculateTransactionDepositAndFee: ({ intermediates, destination, openOrders, hasWrapUnwrapSOL, feeCalculator, }: SetupInstructions & {
    hasWrapUnwrapSOL: boolean;
    feeCalculator: FeeCalculator;
}) => TransactionFeeInfo;
interface CalculateTransactionFeeAndDepositParams {
    numOfAtasToBeCreated: number;
    numOfOpenOrdersToBeCreated: number;
}
export declare const calculateTransactionDepositAndFeeV2: ({ numOfAtasToBeCreated, numOfOpenOrdersToBeCreated, }: CalculateTransactionFeeAndDepositParams) => TransactionFeeInfo;
export {};
