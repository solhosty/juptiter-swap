import { BN, Program } from "@project-serum/anchor";
import { PublicKey, Signer } from "@solana/web3.js";
import { PsyStake } from "../psy_stake";
import { DistributionType } from "../types";
export declare const updateRewardPoolInstruction: (program: Program<PsyStake>, rewardPoolAuthority: PublicKey, stakePool: PublicKey, poolId: number, distributionType: DistributionType, rewardPerEpoch: BN, isActive: boolean, payer?: PublicKey) => Promise<import("@solana/web3.js").TransactionInstruction>;
export declare const updateRewardPoolRpc: (program: Program<PsyStake>, rewardPoolAuthority: PublicKey, stakePool: PublicKey, poolId: number, distributionType: DistributionType, rewardPerEpoch: BN, isActive: boolean, signers: Signer[], payer?: PublicKey) => Promise<string>;
//# sourceMappingURL=update_reward_pool.d.ts.map